[project]
name = "cvrp-optimization"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "ortools",
    "pytest"
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"